# ─── YouTube Shorts News Bot — Dependencies ─────────────────

# Google Gemini (Ukrainian generation, translation, and hook text)
google-genai>=1.0.0

# YouTube video downloader with caption support (VTT format)
yt-dlp>=2024.1.0

# HTTP requests (YouTube API, etc.)
requests>=2.31.0

# YouTube Data API (read-only, no OAuth needed for search)
google-api-python-client>=2.130.0
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
httplib2>=0.22.0
